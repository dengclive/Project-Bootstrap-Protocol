{
  "name": "bootstrap-protocol",
  "version": "2.5.0",
  "description": "Deterministically configure Claude Code from a bootstrap.config.yaml (Project Bootstrap Protocol v2.5.0). Bundles a PRD-driven decision-layer interview tool (proposes a validated config for human approval) and the deterministic installer as the mechanical layer.",
  "author": "Project Bootstrap Protocol",
  "commands": "./commands",
  "homepage": "https://example.invalid/bootstrap-protocol"
}
